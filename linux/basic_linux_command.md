# Basic Linux Commands


## Information on system
Open settings:
` gnome-control-center `

Check space on disk:
` du -sh * `

Check memory on pc:
` df -h `
` df -h . `

Check CPU's usage:
` top `
` htop `

Hostname:
` hostname `

Drivers info:
` ifconfig `